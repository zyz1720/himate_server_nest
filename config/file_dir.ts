import { join } from 'path';

export const FILE_DIR = {
  UPLOAD: join(__dirname, '../../', '/public/uploads'),
  THUMBNAIL: join(__dirname, '../../', '/public/thumbnail'),
  RECYCLE_BIN: join(__dirname, '../../', '/public/recycle_bin'),
};
